# Specification - Advanced Simulation UI

The browser provides time-travel snapshots, GSAP playback, state inspection, split simulations, synchronized controls, and D3 comparison metrics. Nested runs and chaos branches remain independently inspectable.
