# Specification - Import, Export & Data Fidelity

Import/export supports independent and embedded domain models, JSON/YAML, self-contained SVG, metrics and bottleneck bundles, stable keys, shared-reference warnings, and a release-gate round-trip guarantee for exact node positions.
