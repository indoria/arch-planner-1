# Specification - Auth, RBAC & Security Hardening

Authentication is provider-neutral and guarded in NestJS. Shared policies enforce viewer/editor/admin access, while immutable server-side audit records cover privileged actions and OIDC/SAML remains an extension point.
