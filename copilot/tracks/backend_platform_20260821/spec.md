# Specification - Backend Platform & Multi-Tenancy

NestJS exposes modular REST and WebSocket boundaries backed by remote MongoDB. Tenant-scoped repositories, indexes, architecture/domain/run/report persistence, health checks, and deployment-mode metadata are foundational requirements.
