# Specification - Domain & Component Knowledge System

Domains are versioned collections of validated component definitions. A definition has typed sockets, properties, behavior and required cost models. JSON/YAML loading, diagnostics, repository storage, and browsing are independent of the browser renderer and persist through IndexedDB or tenant-scoped MongoDB.
