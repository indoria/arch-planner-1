# Specification - Domain Plugin SDK & Marketplace

The SDK validates manifests and sample architectures, simulates locally, and defines reviewed trust tiers. Untrusted behavior runs without ambient network/filesystem capability and marketplace publication is gated by validation and review.
