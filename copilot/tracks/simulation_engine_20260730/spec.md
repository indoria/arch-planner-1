# Specification - Simulation Engine Core

A deterministic TypeScript event engine runs behavior models with seeded latency, jitter, errors, cost, parallel channels, ripple/back-pressure, recursion limits, and immutable chaos branches. Workers protect the browser UI; server execution uses the same domain contracts.
