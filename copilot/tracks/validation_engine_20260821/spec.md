# Specification - Validation & Verification Engine

One rule engine continuously evaluates structural and domain rules and feeds a Problems panel. Optional read-only verification connectors compare modeled data with live systems only for domains that explicitly support them.
