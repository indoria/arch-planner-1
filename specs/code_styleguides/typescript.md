# JavaScript Migration Note

This legacy filename is retained to preserve the conductor tree. The frontend uses vanilla JavaScript rather than TypeScript. Follow `javascript.md`; use JSDoc for editor hints and validate external data at runtime.
